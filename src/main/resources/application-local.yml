# Explicitly opted-in demo settings. Never combine this profile with production.
spring:
  datasource:
    password: ${DB_PASSWORD:gateway}
gateway:
  bootstrap:
    local-enabled: true
    api-key: ${LOCAL_API_KEY:test-api-key-1}
    ollama-base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
